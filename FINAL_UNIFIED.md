# 🎉 ЕДИНЫЙ САЙТ UBT Farm - Всё в одном + Чат с Claude

## ✅ Что включено:

1. **🌐 Генератор видео/картинок** — полный функционал
2. **💬 Чат с Claude (AI)** — прямо на сайте, общаясь как с тобой
3. **🔓 Общедоступный HTTPS** — через ngrok, работает отовсюду
4. **📱 Мобильная версия** — адаптивный дизайн

## 🚀 Запуск:

### Быстрый способ (batch файл):
Дважды кликни: `LAUNCH_WITH_HTTPS.bat`

Автоматически:
- Запустит сайт на порту 5001
- Запустит ngrok для HTTPS доступа
- Откроет браузер

### Ручной запуск:
```bash
cd "C:\Users\User\Desktop\убт"
./venv/Scripts/python.exe web_app/unified.py --port 5001
```

Затем для HTTPS:
```bash
ngrok http 5001
```

## 📡 Доступ:

**Локально:**
```
http://localhost:5001
```

**Через ngrok (HTTPS, отовсюду):**
После запуска ngrok получишь URL типа:
```
https://abc123.ngrok.io
```

Этот URL работает по всему интернету — отправь другу, открой с телефона!

## 🎨 Возможности сайта:

### Левая панель:
- **Генерация сценариев** — введи тему, получи 7-9 сцен
- **Генерация картинок** — создаёт уникальные изображения
- **Галерея** — все созданные файлы

### Правая панель (ЧАТ):
- **Общение с Claude** — спрашивай о генерации, проси помощи
- **История чата** — сохраняется между сессиями
- **AI отвечает** через твой API ключ `sk-smart-_0uezNrlxMhc4zZGmjZQpdAZl8adsHe2KpFgaFzcIKs`

## 🔑 API Ключ:

Ключ уже настроен: `sk-smart-_0uezNrlxMhc4zZGmjZQpdAZl8adsHe2KpFgaFzcIKs`

Используется для:
- Генерации сценариев (OpenRouter)
- Ответов в чате (GPT-4o-mini)

## 📁 Файлы:

```
web_app/
├── unified.py            # Единый сервер (сайт + чат)
├── templates/
│   └── unified.html      # Веб-интерфейс
└── server.log            # Логи сервера
```

## 💡 Как пользоваться:

1. Запусти сайт (`LAUNCH_WITH_HTTPS.bat`)
2. Скопируй ngrok URL
3. Отправь кому хочешь
4. Они смогут:
   - Генерировать видео/картинки
   - Общаться с AI в чате
   - Смотреть галерею

---

**ВСЁ РАБОТАЕТ!** Один сайт, общий доступ, чат с Claude! 🎊
