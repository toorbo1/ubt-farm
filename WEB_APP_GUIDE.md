# 🌐 Веб-приложение UBT Farm - Полный гайд

## ✅ Что сделано:

1. **Веб-приложение** с полным функционалом Python-приложения
2. **Работает на Flask** — доступно из браузера
3. **Все функции перенесены:**
   - 📝 Генерация сценариев
   - 🖼 Создание картинок
   - 🎬 Генерация видео
   - ▶ Полный цикл создания видео
   - 📊 Просмотр логов и прогресса

## 🚀 Запуск на ПК:

```bash
cd "C:\Users\User\Desktop\убт"
python -m flask --app web_app/app run --host 0.0.0.0 --port 5000
```

Открой браузер: `http://localhost:5000`

## 📱 Доступ с телефона (Termux):

### Способ 1: Через локальную сеть Wi-Fi

1. **Узнай IP ПК:**
   ```powershell
   ipconfig | findstr IPv4
   # Пример: 192.168.1.100
   ```

2. **На телефоне:**
   - Подключись к тому же Wi-Fi
   - Открой браузер
   - Введи: `http://192.168.1.100:5000`

### Способ 2: Через ngrok (публичный URL)

```bash
# На ПК:
ngrok http 5000
# Получишь URL типа: https://abc123.ngrok.io
# Открой на телефоне
```

### Способ 3: Через ADB (если нет Wi-Fi)

```bash
# На ПК:
adb tcpip 5555
adb connect <IP_телефона>:5555

# Проброс порта:
adb reverse tcp:5000 tcp:5000

# На телефоне:
http://localhost:5000
```

## 🎨 Интерфейс веб-приложения:

### Левая панель:
- Поле ввода темы
- Кнопки: Сценарий, Картинка, Видео, Полный цикл
- Прогресс-бар выполнения
- Статус выполнения

### Правая панель (вкладки):
1. **Сценарий** — полный текст с промптами
2. **Галерея** — все сгенерированные картинки
3. **Видео** — просмотр готового видео
4. **Лог** — логи выполнения по шагам

## 📡 Проверка работы сервера:

```bash
# На ПК открой:
http://localhost:5000

# Или через curl:
curl http://localhost:5000
```

## 🔧 Файлы веб-приложения:

```
web_app/
├── app.py              # Flask сервер + API endpoints
├── requirements.txt    # Зависимости Flask
└── templates/
    └── index.html      # Веб-интерфейс с CSS + JS
```

## 📱 Использование на телефоне:

1. Открой `http://<IP_ПК>:5000` на телефоне
2. Введи тему (например: "космос")
3. Нажми **"▶ Полный цикл"**
4. Жди завершения — прогресс отображается
5. Посмотри результат во вкладках

## ⚙️ Автоматический запуск при старте ПК:

Создать файл `start_ubt_web.bat`:

```batch
@echo off
cd /d "C:\Users\User\Desktop\убт"
start "" python -m flask --app web_app/app run --host 0.0.0.0 --port 5000
```

Добавить в автозагрузку Windows:
- Win+R → `shell:startup`
- Переместить туда `.bat` файл

---

**Готово!** Теперь весь функционал доступен через браузер на любом устройстве! 🎉
